#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include<string.h>
#include<math.h>


int main()
{
	int i,n,;
	doubble arr[100] = {0},temp;
	double* pa = arr;
	printf("how many numbers you want to sort");
	scanf("%d", &n);
	printf("input numbers you want to sort");
	for (i = 0; i < n; i++)
	{
		scanf("%f", pa + i);
	}
	for (i = 0; i < n; i++)
	{
		for (j = 0; j < n; j++)
		{
			printf();
			scanf();
			printf();
			scanf();

		}
	}
}
